{
    "compilerOptions": {
        /* Visão Geral */
        "target": "ESNext", // Usa a versão mais recente do ECMAScript
        "module": "NodeNext", // Módulos ESM nativos do Node.js
        "moduleResolution": "NodeNext", // Resolução de módulos moderna
        "esModuleInterop": true, // Melhor interop com módulos CommonJS
        "strict": true, // Habilita todas as verificações de tipo estritas
        "skipLibCheck": true, // Pula verificação de tipos nas declarações de tipo
        "forceConsistentCasingInFileNames": true, // Garante consistência no case dos nomes de arquivos
        "pretty": true, // Formata a saída do compilador
        "newLine": "lf", // Define o caractere de nova linha
        "downlevelIteration": true, // Suporte a iteração em targets mais antigos

        /* Emissão */
        "declaration": false, // Não gera .d.ts no desenvolvimento
        "declarationMap": false, // Não gera sourcemaps para .d.ts no desenvolvimento
        "sourceMap": true, // Gera arquivos .map
        "outDir": "./dist", // Diretório de saída
        "removeComments": true, // Remove comentários
        "inlineSourceMap": false, // Gera sourcemaps separados
        "inlineSources": true, // Inclui fontes originais nos sourcemaps
        "noEmitOnError": true, // Não emite arquivos em caso de erros
        "allowJs": true, // Permite a compilação de arquivos JavaScript
        "checkJs": false, // Desativa a verificação de tipo em arquivos JavaScript

        /* Decorators e Metadados */
        "experimentalDecorators": true, // Suporte a decorators
        "emitDecoratorMetadata": true, // Emite metadados para decorators

        /* Resolução de Módulos */
        "baseUrl": ".", // Diretório base para resolução de módulos
        "paths": {
            "@/*": ["./src/*"], // Mapeamento de caminhos principal
            "@kodus/flow/*": ["src/*"], // Alias para compatibilidade
            "@agents/*": ["src/agent/*"], // Agentes
            "@core/*": ["src/core/*"], // Núcleo do framework
            "@tools/*": ["src/tools/*"], // Ferramentas
            "@workflows/*": ["src/workflow/*"], // Workflows
            "@human/*": ["src/human/*"], // Interações humanas
            "@telemetry/*": ["src/telemetry/*"], // Telemetria
            "@types/*": ["src/types/*"] // Tipos compartilhados
        },

        /* Recursos Experimentais */
        "useDefineForClassFields": true, // Usa define para class fields
        "moduleDetection": "auto", // Detecção automática de módulos
        "verbatimModuleSyntax": false, // Disable to fix import issues

        /* Otimizações de Build */
        "incremental": true, // Compilação incremental
        "composite": false, // Desabilitado para evitar conflitos
        "tsBuildInfoFile": "./dist/.tsbuildinfo", // Local do arquivo de cache de build

        /* Tipagem */
        "lib": ["ESNext"], // Bibliotecas padrão mais recentes
        "types": ["node"], // Tipos globais incluídos
        "typeRoots": [
            "./node_modules/@types", // Tipos de dependências
            "./src/types" // Tipos personalizados do projeto
        ],

        /* Configurações de Linting */
        "noUnusedLocals": true, // Avisa sobre variáveis locais não utilizadas
        "noUnusedParameters": true, // Avisa sobre parâmetros não utilizados
        "noImplicitReturns": true, // Avisa sobre retornos implícitos
        "noFallthroughCasesInSwitch": true, // Avisa sobre fallthrough em switch
        "noImplicitAny": true, // Erro em tipos 'any' implícitos
        "strictNullChecks": true, // Verificações estritas de null/undefined
        "strictFunctionTypes": true, // Verificação estrita de tipos de funções
        "strictBindCallApply": true, // Verificação estrita de bind/call/apply
        "strictPropertyInitialization": true, // Verificação de inicialização de propriedades
        "noImplicitThis": true, // Erro em 'this' com tipo implícito 'any'
        "useUnknownInCatchVariables": true, // Usa 'unknown' em vez de 'any' para catch
        "exactOptionalPropertyTypes": false, // Too strict for current codebase
        "noUncheckedIndexedAccess": true, // Adiciona 'undefined' a acessos por índice
        "allowSyntheticDefaultImports": true, // Permite imports de default sintéticos
        "resolveJsonModule": true, // Permite importar JSON
        "isolatedModules": true, // Garante que cada arquivo pode ser transpilado isoladamente
        "allowUmdGlobalAccess": true // Permite acessar UMD globais
    },

    /* Configurações de Compilação */
    "include": [
        "src/**/*.ts",
        "src/**/*.tsx",
        "src/**/*.js",
        "src/**/*.jsx",
        "tests/**/*.ts",
        "examples/**/*.ts"
    ],

    "exclude": [
        "node_modules", // Exclui a pasta node_modules
        "dist",
        "**/*.d.ts",
        "**/*.test.ts",
        "**/*.spec.ts",
        "**/__tests__",
        "**/__mocks__"
    ]
}
